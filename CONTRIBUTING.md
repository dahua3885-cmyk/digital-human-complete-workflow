# 参与贡献

感谢改进数字人完整流程 Skill。提交 Issue 或 Pull Request 前，请先阅读以下规则。

## 基本要求

1. 不提交任何真人声音、肖像母片、证件、客户数据、访问令牌、模型权重或本机绝对路径。
2. 不移除授权核验、合成标识、文案/音频/画面确认闸门、任务订单版本链或 SHA-256 交接要求。
3. 新增阶段适配器必须符合 [阶段适配器合同](references/stage-adapter-contract.md)，并如实声明依赖、许可证、输入、输出和失败原因。
4. 不把仅能在作者电脑运行的配置写成通用默认值；可配置项应提供不含隐私的示例文件。
5. 不宣传或优化诈骗、冒用、侵权、违法生成和规避平台标识的能力。

## 提交前校验

在仓库根目录运行：

```powershell
python scripts/verify_distribution.py .
```

如果改动了运行配置或适配器，再用不含真人隐私的测试配置运行：

```powershell
python scripts/preflight_runtime.py <测试runtime-config.json>
```

Pull Request 应说明改动目的、受影响入口、验证结果和仍存在的限制。影响用户话术或建档字段时，请同时给出第一次使用的完整模拟结果。

## 贡献许可

除非你明确书面说明，否则你有意提交并被项目接收的贡献将依照 Apache License 2.0 授权，不附加额外条款。
