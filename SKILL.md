---
name: digital-human-complete-workflow
description: Coordinate or partially run an authorized digital-human workflow across source-video rewrite, voice generation, mouth-only avatar video, and one fixed portrait information-layout packaging stage. Use for 完整数字人流程、给视频链接二创后做数字人、直接用文案做数字人、确认音频后做数字人画面、直接把现成数字人口播视频做包装, or resuming from a verified handoff. This skill routes and validates stages; it does not replace the configured production skills.
metadata:
  version: "0.9.2-draft"
---

# 数字人完整流程 Skill

把这项工作视为四个可替换阶段组成的串行生产线，同时允许从有合法、可验证输入的任一阶段开始：

`二创 → 数字人音频 → 数字人画面 → 剪辑包装`

本 Skill 管理入口识别、路由、数字人资产中心、任务订单、授权、状态、阶段交接、版本更新链和最终验收。每个阶段的实际执行仍由 Profile 绑定的阶段 Skill 决定；本 Skill 补充所有开源使用者共同需要、但不能写死为某个创作者参数的通用合同。

## 安装后的第一次启用

安装动作本身不得静默执行代码或采集个人资料。第一次调用本 Skill 时先完整读取 [下载、安装与可运行验收](references/installation-and-runtime.md)：

1. 静默运行 `python scripts/verify_distribution.py <Skill目录>`；未出现 `DISTRIBUTION_OK` 时停止并准确报告文件损坏或缺失。校验通过时不得把版本号、发行校验或发行范围写进客户欢迎语。
2. 在用户选择的工作区检查 `profiles/onboarding-status.json`：

- 不存在：完整读取并执行 [首次启用与本地建档](references/first-run-onboarding.md)。可用 `python scripts/init_user_workspace.py <工作区> --modules all --creator-profile quick` 创建安全骨架，再通过对话完成资料。
- 已存在：读取 [数字人资产中心与四模块串联](references/asset-center-and-module-chaining.md)，显示创作者、数字人音频、数字人形象、剪辑包装四张资产卡的 `ready / pending / needs_recalibration / needs_review / not_selected / blocked` 状态，只补当前入口真正缺少或需要重新校准的资料。
- 首次只选择基础信息简洁版或专业版；声音、形象和包装资料在实际进入对应模块时再建档。未建立声音或形象资料不得阻塞单独二创或剪辑包装入口，但进入相关数字人阶段前必须完成该模块建档。
- 初始化工作区时同时建立二创、数字人音频、数字人形象、剪辑包装四个独立反馈文件；完整读取 [用户反馈沉淀与品牌资料](references/feedback-and-brand-assets.md)。

独立 Skill 没有安装完成后主动发送聊天消息的安装钩子，不得声称“安装瞬间自动弹窗”。第一次点击、显式调用或由任务隐式触发本 Skill 时，先确认当前 Codex 活动任务工作区，运行 `python scripts/prepare_first_use_docs.py <当前活动工作区> --format json`。该脚本把安装目录中的两份最新版客户文档复制到工作区内版本化的 `数字人完整流程资料/` 目录，并返回两个本地绝对路径、GitHub 在线备用链接和固定欢迎语。若当前环境提供 `open_in_codex` 或等效的 Codex 文件打开能力，必须先用返回的 `manual_path`、`profile_path` 主动打开两个工作区副本；没有该能力时跳过主动打开，但仍发送在线可点击链接。脚本会复用内容相同的副本；若发现同版本文件已经被用户修改，则另建目录，不覆盖用户内容。不得直接打开或链接 `.codex/skills/` 安装目录。首次引导不得要求用户在“数字人全流程 / 分别制作”之间二选一。Skill 本身不创建独立工作台。

首次欢迎语是客户可见的固定输出协议，不是参考结构。主动打开两个工作区副本后，必须逐字发送脚本 JSON 中的 `welcome_markdown`，不自行重写。它保留固定三段文字，并把两个文档名称链接到 GitHub 公共在线页，作为不同操作系统和文件查看器的备用入口；客户可见名称不得改变。只有用户明确处于离线环境时，才改发脚本提供的 `local_welcome_markdown`。不得自己猜测路径，不得回退到 Skill 安装目录。不得在三段之前、之间或之后增加任何文字，不得显示版本号、`draft`、发行校验、运行配置、阶段绑定、工作区初始化、端到端状态、内部限制或其他技术说明。内部校验成功必须静默；只有发行文件实质损坏、没有可用活动工作区且在线文档也不可用，才可以不用固定欢迎语并准确报告阻塞。

```markdown
数字人完整流程 Skill 已安装完成。

为了更好地使用，请先阅读“数字人完整流程使用手册”，再填写“开始使用前请先填写”。

资料表中有基础信息简洁版和专业版，请选择一个版本填写。填写完成后，复制所选版本的全部文字并发送给我，我会为你建立个人资料。
```

四阶段 `runtime-config.json` 和端到端运行预检不属于首次欢迎语。收到用户资料或用户实际发起制作任务后，再在进入相关生产阶段前静默运行 `preflight_runtime.py`；只有该用户请求的阶段确实不可运行时，才用客户能理解的语言说明缺少什么和下一步，不展示发行草案术语。

用户把填写后的资料复制回聊天后，识别其版本并按“一次填写即可建档”处理。简洁版已经包含建档所需的职业、领域、受众、受众问题、内容方向、内容目标、表达风格、真实材料、禁用项和专业边界；用户逐项作答，或对可选项明确填写“无 / 暂无”，即视为资料完整。不得再拿专业版、内部开工清单或执行者认为“最好有”的信息追加第二套问卷；不得再次询问受众的三个问题、3–5 个内容方向、公开案例、动态事实确认方式、保存范围或“确认按此建档”。动态事实默认由 AI 在每条内容制作时核验公开来源，只有来源冲突、高风险或无法公开核验的具体事实才针对该事实询问；资料默认仅保存在使用者本机并长期复用，用户可随时要求改为仅本项目、修改或删除。

收到完整回传后，直接把用户原始答案与 AI 可明确推导的结构化摘要写入 `creator-profile.md`，然后简短告知“个人资料已建立，数字人全流程和分别制作均可使用”。AI 可以主动整理核心价值、受众画像或内容支柱，但这些属于可随时修改的辅助总结，不能转化为新的必答题。只有必填项确实留空、答案互相矛盾或涉及不能安全推断的资质与授权时，才只询问那个具体缺口，并在问题中附一个填写示例；不能让用户重填整份资料。只有用户明确要求访谈代填时，才在聊天中逐项提问。初始化后《数字人完整流程》和《开始使用前请先填写》都位于用户工作区根目录，`forms/` 同时提供创作者资料简洁版、专业版和合法使用授权声明。

首次启用只做一次完整告知，形成长期本地资料库；每条新视频只收来源、文案、用途、动态事实和本条素材。所有真人声音、肖像与授权仍按阶段最小化提交，不在安装时自动上传。

当前公开包的 `distribution-manifest.json` 标记为 `end_to_end_runtime_included=false`：它能验证总控发行文件，但不包含四个通用阶段 Skill、模型/服务和真实授权素材。正式仓库补齐这些依赖并通过端到端演示前，只能称发行草案，不能承诺所有电脑下载即完整生成。此信息是维护者和生产阶段的内部边界，不得出现在首次客户欢迎语中。

## 开工前

1. 完整读取 [入口路由](references/entry-routing.md)、[阶段合同](references/stage-contracts.md) 与 [责任边界和滥用处理](references/maintainer-liability-and-abuse.md)。
2. 先识别本次是纯本地开源运行、使用者自建服务，还是维护者提供的托管/代制作。不得把纯开源免责声明自动套给托管或代制作业务。
3. 读取首次启用状态；首次调用必须先用 `prepare_first_use_docs.py --format json` 把两份最新版文档准备到当前活动工作区，有 Codex 文件打开能力时主动打开两个本地副本，再严格发送脚本返回的固定欢迎语和两个 GitHub 在线备用链接。不得把文档正文或内部技术状态铺在聊天中。收到用户复制回传的资料后，按上方“一次填写即可建档”规则直接建立创作者资料；[开工资料清单](assets/preflight-intake-checklist.md) 只用于用户实际发起某条制作任务后的项目资料和分阶段素材，不得反过来追加创作者问卷。
4. 让用户完成 [合法使用与授权声明](assets/lawful-use-and-consent-declaration.template.md)。拒绝声明、用途含糊、授权无法证明或出现高风险冒用迹象时停在 `preflight`；声明不是免责万能证明，也不能覆盖已经可见的异常。
5. 找到项目指定的 `workflow-profile.json`。没有时，从 [示例 Profile](assets/workflow-profile.example.json) 复制到项目目录并完成绑定；字段解释见 [Profile 规范](references/profile-schema.md)。
6. 运行 `python scripts/validate_workflow_profile.py <workflow-profile.json>`。结构校验失败时停止，不得猜测阶段 Skill。
7. 在项目目录建立 `skill-contract.md`，并按当前环境的上级规则补齐合同内容。
8. 建立 `project-brief.md`，记录本次主题、目标平台、语言、人物、声音与肖像授权证据、输入真源、品牌、交付要求和隐私边界。检查本地 `creator-profile.md`；二创或文案适配需要时，让用户在 [简洁版](assets/creator-profile-quick.template.md) 与 [专业版](assets/creator-profile-pro.template.md) 中选择。用户不愿填写时可用访谈方式代填，但不得虚构答案。公开 Profile 不保存真人参考声、肖像素材、令牌或密钥。
9. 完整读取 [任务订单、预计时间与版本更新](references/task-order-and-estimates.md)，使用 `create_task_order.py` 建立本条 `task-order.json`，记录 Profile/资产中心快照哈希、入口、目标、四阶段 Skill、当前状态、输入、交接物、预计时间、失败原因和下一动作。阶段必须串行执行，不得并行启动依赖后续输入的制作。
10. 任何阶段开始前只向用户显示简单的大致时间，例如“数字人音频预计约 8–12 分钟”。内部可以保存估算依据，但界面不要求用户阅读复杂参数。网络不可用时直接显示“网络异常，恢复后继续”，不显示生成失败按钮；生成完成但效果一般时标记“待优化”，不称文件损坏。

若用户从中间阶段开始，只跳过已经存在且通过哈希、授权和 QA 验证的上游阶段。外部文案、音频或视频没有本流程交接物时，为该输入建立 `external-*-handoff.json`，记录用户提供事实、授权声明、媒体检查与 SHA-256；不得伪装成上游 Skill 已执行。

## 两种用户使用场景

### 1. 数字人全流程

视频链接、本地视频、音频或逐字稿 → 二创 → 文案确认 → 数字人音频 → 音频确认 → 数字人画面 → 画面确认 → 剪辑包装。继续提示放在每个对应阶段完成处，用户确认继续时自动读取交接物。

### 2. 分别制作

按生产顺序向用户展示三个可独立使用的模块：

1. **二创文案**：把视频链接、本地视频、逐字稿或资料制作成符合其个人资料的原创口播文案；交付后可结束，也可继续数字人音频。
2. **数字人**：用户提供最终文案时按“数字人音频 → 用户确认 → 数字人画面”执行；已有最终确认音频时可验证后只做画面；用户明确只要其中一项时在对应交付后停止。
3. **剪辑包装**：用户提供有权使用的现成数字人或单人口播视频 → 媒体体检、转写与素材授权 → 直接执行固定包装和封面。

“数字人全流程”和“分别制作”是两种随时可用的任务方式，不是首次建档时永久选择的套餐、权限或互斥模式。只有用户实际发起某次制作任务时，才根据其输入和目标识别本次入口。

入口识别只决定从哪里开始，不降低所进入阶段及其后续阶段的合同。用户同时给出多种输入时，以最后明确点名的主输入为唯一真源。单模块任务完成后，用户选择继续下一模块时，使用同一任务订单的 `extend` 操作延长目标阶段；不得让用户复制粘贴上一步文案、音频或视频。

## 阶段执行规则

进入每个阶段前，主执行者必须重新完整读取 Profile 指定阶段 Skill 的 `SKILL.md`，再读取其中标记为必读、开工前读取或与选定版本直接相关的引用。不得凭本 Skill 的摘要代替阶段 Skill。

### 1. 二创

- 完整读取并执行 [通用二创流程](references/generic-rewrite-workflow.md)，再执行 `stage_skills.rewrite.skill` 中不与用户要求冲突的更严格规则。
- 二创必须先读取 `creator-profile.md`，按人物真实职业、受众、内容领域、证据口径、表达样本、真实案例和禁用项适配。已完成简洁版即达到通用二创最低资料要求；可选样本或案例填写“无 / 暂无”不属于资料不足，必须采用保守、不虚构的通用表达继续。只有当前主题涉及具体资质、私有事实或高风险主张且现有资料无法判断时，才针对该单项提问；不能要求补填专业版或第二套资料。不能把 AI 创作者口吻推广给律师、教师、医生或其他行业。
- 用来源视频、链接、逐字稿或用户材料建立来源证据，先理解原命题，再独立完成同款内容研究和领域权威事实核验。
- 输出至少包含来源记录、原视频选题卡、同款候选、来源贡献表、重构判断、干净口播稿、原创性/事实/可制作性 QA 与反馈记录。
- 口播稿只有在用户明确确认，或用户原样提供并明确要求直接使用时，才能生成 `handoffs/script-handoff.json` 并进入声音阶段。
- 登记交接物后，任务订单自动把其路径和 SHA-256 写成数字人音频输入。先显示完整的已确认口播文案，并紧接着用一级 Markdown 标题单独显示 `# 是否继续生成数字人音频？`。标题不得降级成普通文字、小标题或埋入段落。
- 大标题下只给两个明确选项：`继续生成数字人音频`、`复制文案并结束任务`。复制已确认的干净口播稿不创建新版本，也不改变订单状态；用户选择继续时自动读取文案交接物，不要求复制粘贴。
- 用户对二创的修改意见写入 `feedback/rewrite-feedback.md`，并区分仅本条、系列偏好或经确认的长期规则。

### 2. 数字人音频

- 首次人物声音未建档或明确需要重新校准时，完整执行 [数字人音频首次建档](references/audio-onboarding.md)，并读取 [声音个体化校准](references/voice-calibration.md)、[中英文发音核验](references/pronunciation-research.md) 与 `stage_skills.voice.skill`。已有有效 `voice-profile.json` 时只执行其中“每条视频的数字人音频”，不重复三风格选择。
- 三风格确认的作用域固定为一个有效 `voice-profile.json`：只在首次建档选择一次。以后每条视频必须自动沿用 `selected_style`，不得再次询问、生成三风格或因普通换稿改变风格。只有人物、模型、参考声或语言变化导致原参数不再适用于当前组合时，才建立新版本并重新校准；旧 Profile 保留为历史版本，不称文件损坏。
- 参考声录制前先发送 [声音参考录制指南](assets/voice-recording-guide.md)。通用母素材建议 2–5 分钟，实际模型片段长度和格式服从被绑定声音 Skill；不把通用建议冒充某个模型官方要求。
- 首次建档必须用同一段校准文案生成“自然均衡、稳健融合、短视频紧凑”三种候选，让用户试听后只选一种；选定风格写入 `voice-profile.json`，后续默认沿用。不得把“短视频紧凑”宣称为有权威统计支持的最多人使用版本。
- 不把任一人物的 `0.x` 速度、停顿、seed、模型或参考声当作通版默认。每个“人物 + 模型 + 参考声 + 语言”组合先生成校准候选，由用户确认后冻结 `voice-profile.json`；模型或参考声改变时重新校准。
- 每条视频先冻结显示稿，再处理发音词典和 TTS 稿；先试听开头、最长句、英文/数字风险句，通过后生成完整音频。完成 ASR 对稿、响度、静音、重复、吞字、爆音和连续试听 QA；不得同时启动数字人画面。
- 完整音频必须由用户实际试听并明确确认。技术成功、ASR 正确或“默认可以”都不能代替确认。
- 区分“风格确认”和“成片音频确认”：风格只确认一次；每条视频仍需确认该条完整音频是否正确，但这个确认不能改写长期风格。用户要长期换风格时，创建新版本的声音 Profile 并记录生效时间，不能只改当前一条。
- 确认后生成或接收阶段 Skill 的批准单，并把最终音频 SHA-256 写入 `handoffs/audio-handoff.json`。
- 登记后自动成为数字人画面的输入，并用一级 Markdown 标题显示 `# 是否继续生成数字人画面？`，下方只提供“继续生成数字人画面 / 结束任务”。
- 用户对读音、停顿、节奏、情绪或音质的修改意见写入 `feedback/voice-feedback.md`；单条意见不得自动改变首次确认的长期音频风格。

### 3. 数字人画面

- 完整读取 [参考视频录制与体检标准](references/avatar-capture-standard.md)、[授权和合成标识](references/consent-and-disclosure.md) 与 [私有资料存储](references/storage-and-scale.md)，再执行 `stage_skills.avatar.skill`。
- 在用户开始录制前，必须先发送 [录制前检查清单](assets/avatar-recording-checklist.md)，解释时长、构图、动作、光线、背景和拒收项，并取得用户已知悉的确认。用户已有素材时不补录确认，但仍按同一标准体检并说明风险。
- 开始前重新计算音频哈希，验证声音与肖像的单独授权，并对真人参考视频执行素材体检。杂乱背景不是自动失败，但动态背景、多人、镜头移动、遮脸、强头部运动、失焦、曝光跳变、硬切或脸部检测失败均不得直接进入“只改嘴部”路线。
- 阶段 Skill 不得修改已批准音频；任何音频变化都必须退回声音确认。
- 完成其规定的内部代表性样片、口型/动作/接缝/封装 QA 后，先把完整数字人画面交给用户观看并停止。
- 只有用户明确确认该完整画面可进入包装阶段，才生成 `handoffs/avatar-handoff.json`。技术 QA 不能代替用户画面确认。
- 登记后自动成为剪辑包装输入，并用一级 Markdown 标题显示 `# 是否继续剪辑包装？`，下方只提供“继续剪辑包装 / 结束任务”。
- 用户对构图、口型、动作、背景或自然度的修改意见写入 `feedback/avatar-feedback.md`。

### 4. 剪辑包装

- 执行 `stage_skills.packaging.skill`。人物 MP4、主音频、字幕和文案必须追溯到同一条已验证交接链。
- 包装不提供版式选择。完整读取并固定使用 [剪辑包装](references/packaging.md)：1080×1920，人物固定右下小窗口，上方/中部为主要信息区，黑色背景与青蓝强调，并交付统一封面。公开文档和用户沟通中只称“剪辑包装”，不使用历史版本编号或内部母版名。
- 完整制作前建立文案语义表、口播触发点表、素材真源表和代表性内部样片；用户要求直接完成全片，只能免去等待确认，不能免去这些内部工作。
- 真实 Logo、真实界面、真实人物与指定素材必须按阶段 Skill 的证据等级使用；缺失时停止或明确降级，禁止伪造。
- 对最终文件本身完成技术与人工 QA，再生成 `delivery-manifest.json`。
- 用户对封面、字幕、素材、信息层级和 CTA 的修改意见写入 `feedback/packaging-feedback.md`。品牌资料只保存账号/品牌名称、Logo、授权字体与颜色、固定 CTA、必要声明、封面素材及授权；个人创作者没有 Logo 时允许留空。

## 状态与回退

只使用以下状态：

`preflight → rewriting → script_waiting_approval → voicing → audio_waiting_approval → avatar_rendering → avatar_qa → avatar_waiting_approval → packaging_sample → packaging → final_qa → delivered`

- 用户输入或授权不完整：停在 `preflight`。
- 文案未确认：停在 `script_waiting_approval`。
- 音频未明确确认：停在 `audio_waiting_approval`，这是不可自动跨越的硬闸门。
- 完整数字人画面未明确确认：停在 `avatar_waiting_approval`，不得自动进入包装。
- 阶段 Skill 缺失、必读文件缺失或接口不兼容：停在当前阶段，报告准确缺口；不得创建同名假 Skill 代替。
- 任一上游内容变化：按 [阶段合同](references/stage-contracts.md) 的版本一致性矩阵把相关阶段标记为 `needs_update`。旧文件仍保留且可播放，只作为历史版本；旧确认继续绑定旧哈希，不能自动批准新版本。

## 授权与隐私

- 只处理用户有权使用的声音、肖像、视频、音乐、商标、界面和其他素材。
- 不能仅凭文件在本机存在就推定授权。每名被合成的人物都必须有独立的 `consent-record.json` 或等效证据，分别记录声音、肖像、允许用途、平台、商业使用、期限、是否允许转授权/训练及撤回方式；本人使用也记录自我声明。
- 发布时主动声明生成合成内容，并保留平台标识或文件元数据标识；不得删除、篡改或隐匿合成标识。
- 克隆声、真人母片、身份文件、Cookie、令牌、私有路径和未脱敏业务素材不得进入开源包。
- 开源默认采用本地私有存储：授权声明与证据存放在每位使用者自己的项目私有目录或其自行配置的对象存储中，开源维护者不接收、不集中保存。只有另行建设托管服务并完成租户隔离、加密、最短留存、删除/撤回和审计设计后，才允许开启集中云存储。
- 用户明确要求的一键或无人值守流程，不会扩大授权范围，也不会取消人工音频确认。

## 违法用途与冒用阻断

- 不制作或协助制作诈骗、冒充身份索财、伪造官方/新闻/司法内容、诽谤、威胁、非法色情或其他违法侵权内容；不提供绕过授权、标识、平台审核或追溯的办法。
- 被合成人物不是使用者本人时，必须取得可验证的声音与肖像单独授权。第三方授权至少包含书面/电子声明和能绑定本项目用途的证据；仅有一张照片、公开视频、聊天口头转述或“对方同意了”不足以进入制作。
- 公众人物、未成年人、政府/司法/金融/媒体/客服身份、收款或账号验证场景、要求删除 AI 标识、要求隐瞒合成事实等属于高风险。默认停止，只有在用途合法、授权充分并完成适用法律专业复核后才可重新评估。
- 发现违法或冒用迹象后停止当前运行，不继续排错、改提示词、换模型或提供替代路径。纯本地运行只在本地记录安全决定；托管服务按 [责任边界和滥用处理](references/maintainer-liability-and-abuse.md) 执行账户限制、证据留存、投诉和依法报告。
- 开源维护者免责声明不能替代上述闸门，也不能排除适用法律下不可排除的责任。

## 交付

开源维护与发布还须读取 [开源产品化路线](references/open-source-productization.md)、[当前流程缺陷和优化优先级](references/workflow-optimization-audit.md)、[0.8 剩余明显 Bug 审计](references/bug-audit-0.8.md) 与 [现有阶段依赖审计](references/current-stage-dependency-audit.md)。交付前完整读取并执行 [开源与成片验收表](references/release-checklist.md)。最终回复必须说明：

- 实际绑定并执行的四个阶段 Skill 及可识别版本。
- 本次从哪个入口开始，以及哪些阶段因可信外部输入被合法跳过。
- 已完成的阶段、批准单、成片与配套文件。
- 文案、音频、数字人画面三次用户确认，最终技术 QA、人工 QA 和哈希链结果。
- 未完成项、降级项、授权或素材限制。

任一被绑定阶段 Skill 的必交物缺失时，不得称为“完整流程已完成”或“最终版”。
